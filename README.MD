#### PTE using Docker

```
# clone the repo
git clone git@github.com:Ceceu/pte_docker.git && cd pte_docker

# create a docker image
./create_images.sh

# run docker container
./run_container.sh

# inside container execute the PTE test run
./run.sh
```
