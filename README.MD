#### PTE using Docker

```

# clone the repo
git clone

# create a docker image
./create_images.sh

# run docker container
./run_container.sh

# inside container execute the PTE test run
./run.sh
```
